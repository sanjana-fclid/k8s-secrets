apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ .Release.Name }}-config
  namespace: {{ .Values.namespace }}
data:
  # Key-value pairs for configuration
  my-config-value: {{ .Values.config.myConfigValue | quote }}
  another-config-value: {{ .Values.config.anotherConfigValue | quote }}
