apiVersion: apps/v1
kind: Deployment
metadata:
  name: env-test
  namespace: env-test
  labels:
    app: env-test
spec:
  replicas: 3
  selector:
    matchLabels:
      app: env-test
  template:
    metadata:
      labels:
        app: env-test
    spec:
      imagePullSecrets:
        - name: dockerhub-secret
      containers:
        - name: env-test
          image: sanjanafclid/envtest:latest
          ports:
            - containerPort: 8000
